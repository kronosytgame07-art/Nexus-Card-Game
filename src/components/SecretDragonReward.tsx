import { useGame } from '../store/game';
export function SecretDragonReward(){const pending=useGame(s=>s.dragonStructureRewardPending);const close=useGame(s=>s.consumeDragonStructureReward);if(!pending)return null;return <div className="signin-gate-overlay" role="dialog" aria-modal="true"><div className="signin-gate-card"><small>RÉSONANCE INCONNUE</small><b>Le Nexus répond.</b><p>Dix victoires classées consécutives ont réveillé un ancien écho. Le deck de structure Dragon rejoint ta collection.</p><button className="primary" onClick={close}>Recevoir le deck</button></div></div>}
